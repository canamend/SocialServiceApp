import { AvatarPatientPipe } from './avatar-patient.pipe';

describe('AvatarPatientPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
